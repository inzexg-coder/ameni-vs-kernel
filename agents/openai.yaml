interface:
  display_name: "Ameni Monitor — System Dashboard"
  short_description: "Real-time Linux system monitoring — CPU, RAM, disks, network in a beautiful web dashboard"
  default_prompt: "Use ameni-monitor to monitor system health in real time. Start with `.ameni/bin/ameni monitor` and open http://localhost:3000."

policy:
  allow_implicit_invocation: true
